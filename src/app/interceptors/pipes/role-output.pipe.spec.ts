import {RoleOutputPipe} from './role-output.pipe';

describe('RoleOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
