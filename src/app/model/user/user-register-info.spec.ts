import {UserRegisterInfo} from './user-register-info';

describe('UserRegisterInfo', () => {
  it('should create analysis instance', () => {
    expect(new UserRegisterInfo()).toBeTruthy();
  });
});
