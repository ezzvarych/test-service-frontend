import {CompetitionOutputWithQuestions} from './competition-output-with-questions';

describe('CompetitionOutputWithQuestions', () => {
  it('should create an instance', () => {
    expect(new CompetitionOutputWithQuestions()).toBeTruthy();
  });
});
