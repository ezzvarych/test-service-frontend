import {CreateNewCompetition} from './create-new-competition';

describe('CreateNewCompetition', () => {
  it('should create an instance', () => {
    expect(new CreateNewCompetition()).toBeTruthy();
  });
});
